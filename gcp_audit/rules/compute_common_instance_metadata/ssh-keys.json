{
    "name": "Found ssh-keys in project metadata",
    "filters": [
        {
            "matchtype": "regex",
            "filter": {
                "key": "ssh-keys|sshKeys"
            }
        }
    ]
}
